"""
Structural recurrence detectors for C2 beacon detection.

Each subpackage exposes a `classify_flow(timestamps, ...)` function
that returns a dict with at minimum 'classification' and 'confidence'.
"""
